
/**
 * _abs - A function that computes the absolute value of an integer.
 * @n: number to compute its absolute value.
 *
 * Return: n if number is positive
 * otherwise -n
*/

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
